import { HealthPlaceholder } from "@/components/health-status";

export default function Loading() {
  return (
    <main>
      <div className="shell">
        <HealthPlaceholder />
      </div>
    </main>
  );
}
